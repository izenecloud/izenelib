MACRO_CHECK_SHOULD_BUILD(drum)

IF (drum_SHOULD_BUILD)

  FILE (GLOB drum_SRC
    *.cpp
    )

  INCLUDE_DIRECTORIES(
    ${Boost_INCLUDE_DIRS}
    ${IZENELIB_SOURCE_DIR}/include
    )

  MACRO_ADD_LIBRARY(drum SRCS ${drum_SRC})

ENDIF (drum_SHOULD_BUILD)
